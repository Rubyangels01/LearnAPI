<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LearnAPI" parent="Theme.Material3.DayNight.NoActionBar"></style>

    <style name="Theme.LearnAPI" parent="Base.Theme.LearnAPI" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="AppTheme" parent="Base.AppTheme">
        <!-- Customize your AppTheme here -->
        <item name="android:fontFamily">@font/aldrich</item>

        <item name="colorOnPrimary">@color/white</item>
    </style>
</resources>
